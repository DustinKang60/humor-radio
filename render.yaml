services:
  - type: web
    name: humor-radio-audio-proxy
    env: docker
    plan: free
    dockerfilePath: server/Dockerfile
    dockerContext: server
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8787
